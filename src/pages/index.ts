import { lazy } from 'react';

export const CountryDetails = lazy(() => import('./CountryDetails'));
export const Home = lazy(() => import('./Home'));
export const NoMatch = lazy(() => import('./NoMatch'));
